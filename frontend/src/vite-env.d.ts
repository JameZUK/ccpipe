/// <reference types="vite/client" />

// Injected at build time by vite.config.ts `define` — short git SHA + UTC
// build time of the running bundle. Shown in Settings → About.
declare const __BUILD_ID__: string;
